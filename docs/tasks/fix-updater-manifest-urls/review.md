# 审查报告 · fix-updater-manifest-urls

审查对象：`.github/workflows/build.yml` 新增的 `rewrite-manifest-urls` 任务、已发布的 v0.9.2 清单热修、`README.md` / `.agents/skills/gitwave-release/SKILL.md` / `docs/tech/engineering/00-overview.md` 的措辞、`docs/tasks/fix-updater-manifest-urls/plan.md`。

审查方法：只读审查（不改文件、不改线上状态）。逐行读 diff；用替身 `gh` + 真实 `jq` 把任务里的脚本原样抽出跑故障注入；对线上 v0.9.2 做只读核对（清单内容、附件列表、标签对象、地址连通性）；核实上游构建工具源码；YAML 解析与格式检查。

## 结论

**可以提交。** 未发现阻断合并的问题。

## 对齐表

| 计划项 | 实现情况 | 结论 |
|---|---|---|
| 新增改写任务（下载 → 建映射 → 改写 → 断言 → 覆盖上传） | `.github/workflows/build.yml` 的 `rewrite-manifest-urls` 任务，`needs` / job 级权限 / 各步骤与计划一致 | 一致 |
| 两道断言（不得残留接口域名、平台条目数大于 0） | 审查后已加强：改为「所有地址必须以 `https://github.com/` 开头」+「三个主平台键齐全」，见下方处理记录 | 一致（有收紧） |
| 已发布的 v0.9.2 清单改写后重传 | 线上核对：`version=0.9.2`、8 个平台键、接口地址 0 个、8 个普通地址不带请求头全部 206；标签对象本地与远端一致（未移动） | 一致 |
| 计划里的「干跑验证、除地址外逐键一致」 | 复现通过：用真实附件信息还原出改写前的清单再干跑，结果与线上已修复清单逐字节相同，版本号 / 更新说明 / 发布时间 / 8 个签名均未变 | 一致 |
| 发布技能与 README 措辞 | 与实现相符，任务名与工作流一致 | 一致 |
| `docs/tech/` 发布流程概述 | 审查时缺失，已补一条「更新清单」 | 一致（已补） |

## 🔴 必须修

无。

审查重点怀疑项均已排除：`needs` 等待的是整个构建任务（含 macOS 公证步骤，该步骤只覆盖上传安装包，与清单无冲突）；草稿发布用默认作业令牌可读可写，同一工作流里已有生产环境同类证据；改写失败时一律不产生上传（见下方故障注入结果）；清单除地址外的字段逐字节保留；上游构建工具没有可配置项能直接产出普通下载地址（源码里写死了接口地址，输入参数也没有相关开关），服务端改写是当前唯一可行手段。

## 🟡 建议修（已全部处理）

1. **注释与代码自相矛盾**：原注释说令牌权限来自工作流级，实际是 job 级覆盖工作流级。已改为写明 job 级权限是前提，并指出历史上把它调成只读会导致草稿不可见（`release not found`）。
2. **地址不必自己拼**：原先用「标签 + 文件名」拼地址，隐含标签与文件名的转义假设。已改为直接用附件列表里的 `browser_download_url` 字段，删掉拼串与 `--arg base`。
3. **断言强度低于人工核对项**：原先只要求「平台条目数大于 0」，放过「只剩 1 个平台」的清单。已补一条三主平台键齐全的断言（与发布技能里的人工核对项对齐）。审查者指出，历史上那个被删除的校验任务两次失败分别源于缺 checkout 与权限设错，本任务都不存在，成本只是一行。
4. **与历史决策的关系未写明**：已在本任务计划里写清这是有意开的例外（只替换地址字段，其余字段仍由构建工具唯一生产）。
5. **已被证伪的旧结论未标注**：已在本任务计划顶部标注作废 `docs/tasks/fix-ci-redundant-publish-job/plan.md:25` 的结论。

## 🟢 可选建议（处理情况）

- 计划里「通过 prettier 检查」对工作流文件不是有效证据（仓库的 `.prettierignore` 排除了 `*.yml`）：已改写为「显式指定文件路径时通过 prettier 检查」并注明这一限制。
- 原先的断言用 `grep` 扫整份清单，若以后更新说明里出现该字符串会误报：已改为只检查 `platforms[].url`，同时覆盖「地址指向其它域名」的情况。
- 建议在计划的「已知限制」里点明「只重跑失败任务不会触发本任务」：已补，并指出发布前人工核对是兜底。
- 建议补 `docs/tech/engineering/00-overview.md` 的发布流程概述：已补。

## 故障注入实测（审查者独立跑出的结果，作者在收紧断言后重跑一致）

| 用例 | 期望 | 实测 |
|---|---|---|
| 接口地址清单（修复前形态） | 改写并上传 | 退出码 0，上传一次，8 个地址全部改写为普通地址 |
| 已是普通地址 | 幂等通过 | 退出码 0 |
| 一个地址指向其它域名 | 拒绝上传 | 退出码 1，报「1 platform URL(s) that are not github.com download links」 |
| 少一个主平台键 | 拒绝上传 | 退出码 1，报「missing darwin-aarch64 / linux-x86_64 / windows-x86_64」 |
| 附件列表为空 | 拒绝上传 | 退出码 1，报「8 platform URL(s)…」 |

## 未覆盖的项（如实说明）

- 未安装 `actionlint`，工作流只做了 YAML 解析与人工核对。
- 没有真实触发一次持续集成运行做端到端验收（需要发版才能触发）。
- 未验证用户报告的应用内 403 原文，只验证了它的技术前提（接口地址在缺请求头 / 额度耗尽时返回 403）。
